<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile Dashboard</title>
   <style>
    /* General Styles */
body {
    background-color:teal;
    font-family:'Arial', sans-serif;
    margin: 0;
    padding: 0;
    color: #333;
}

.container {
    max-width: 900px;
    margin: 20px auto;
    padding: 20px;
    background: rgba(255, 255, 255, 0.6); /* Semi-transparent white */
    border-radius: 8px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2); /* Soft shadow for depth */
    backdrop-filter: blur(10px); /* Frosted glass effect */
    border: 1px solid rgba(0, 0, 0, 0.1); /* Light border */
    overflow: hidden; /* Ensure the border radius works */
}
.header {
    text-align: center;
    margin-bottom: 20px;
}

.username {
    color: teal;
}

.user-info h2 {
    margin: 5px 0;
    font-weight: normal;
}

.profile-content {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.profile-picture {
    background-color: #e0f7fa;
    color: #00796b;
    border-radius: 50%;
    width: 150px;
    height: 150px;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 20px;
}

.profile-picture img {
    border-radius: 50%;
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.profile-actions {
    text-align: center;
}

.stats {
    display: flex;
    justify-content: center;
    margin-bottom: 20px;
    gap: 20px;
}

.stat-item {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.action-button {
    border: none;
    padding: 8px 16px;
    color: #fff;
    background-color: teal;
    border-radius: 5px;
    cursor: pointer;
    font-size: 14px;
    margin-bottom: 5px;
}

.stat-count {
    font-size: 16px;
    color: #555;
}

.actions {
    display: flex;
    gap: 15px;
    justify-content: center;
}

.action-form {
    display: inline;
}

.logout-button, .edit-profile-button {
    border: none;
    padding: 8px 16px;
    color: #fff;
    background-color: teal;
    border-radius: 5px;
    cursor: pointer;
    font-size: 14px;
    text-decoration: none;
}

.logout-button {
    background-color: #d32f2f;
}

.edit-profile-button {
    background-color: #0288d1;
}

   </style>
</head>
<body class="welcome">
    <div class="container">
        <header class="header">
            <h1>Welcome, <span class="username"><%=user.name%></span></h1>
            <div class="user-info">
                <h2>ID: <%=user.id%></h2>
                <h2>Email: <%=user.email%></h2>
            </div>
        </header>
        <div class="profile-content">
            <div class="profile-picture">
                <img src="https://cdn.dribbble.com/users/5534/screenshots/14230133/profile_4x.jpg" alt="Profile Picture">
            </div>
            <div class="profile-actions">
                <div class="stats">
                    <div class="stat-item">
                        <button class="action-button">Following</button>
                        <span class="stat-count">500</span>
                    </div>
                    <div class="stat-item">
                        <button class="action-button">Followers</button>
                        <span class="stat-count">6089</span>
                    </div>
                </div>
                <div class="actions">
                    <form method="get" action="/login" class="action-form">
                        <button class="logout-button">Logout</button>
                    </form>
                    <form method="get" action="/<%=user.id%>?__METHOD=PATCH" class="action-form">
                        <button class="edit-profile-button">Edit Profile</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
